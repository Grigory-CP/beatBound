# Contributing

## Project structure

- `packages/frontend` contains frontend code.
- `packages/backend` contains backend code.

## Formatting

Run this from the repository root before committing:

```bash
npm run format
```
